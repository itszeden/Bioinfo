n <- 10
p_K <- 0.5
p_Z <- 0.5


Münzenwurf <- sample(c("Kopf","Zahl"), n,replace = T, prob = c(p_K, p_Z))
Münzenwurf

s_Kopf <- nchar(Münzenwurf["Kopf"])
s_Kopf

for (i in 1:(n+2)){
  
}

# Laplace: n+2 und s+1


